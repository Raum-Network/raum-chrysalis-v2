[package]
name = "arc_stellar_receipts"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "21.7.7"

[dev_dependencies]
soroban-sdk = { version = "21.7.7", features = ["testutils"] }
